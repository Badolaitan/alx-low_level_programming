#include "main.h"

/**
 * _abs -> return absolute value of a number
 * @n: parameter
 * Description: '_abs code'
 *
 * Return; absolute value of a number
 */
void _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}
